let a = 3123; /* create and set value for a */
let b = 92; /* create and set value for b */
let sum = a + b; /* calculate and store the sum */
let difference = a - b; /* calculate and store the difference */
let product = a * b; /* calculate and store the product */
let quotient = a / b; /* calculate and store the quotient */
let remainder = a % b; /* calculate and store the reminder */
console.log(
  "a=" + a,
  "b=" + b,
  "a+b=" + sum,
  "a-b=" + difference,
  "axb=" + product,
  "a/b=" + quotient,
  "a%b=" + remainder
); /* print the console log of all results */
